name: Print environment variables
run-name: Testing env
on:
  push:
    branches:
      - '**'
    tags:
      - '*'
  workflow_dispatch: {}

jobs:
  debug:
    if: github.repository_owner != 'aws'
    runs-on: ubuntu-latest

    steps:
      - name: Print
        run: env | sort
